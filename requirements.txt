# This file lists the dependencies required for running the project.
# These dependencies are not mandatory but may be required if the code does not run properly.
# To install them, use the command: pip install -r requirements.txt

numpy==2.1.3
matplotlib==3.9.2
seaborn==0.13.2
pandas==2.2.3
